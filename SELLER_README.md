# نسخه فروشنده حساب‌یار

این نسخه برای فروشندگان طراحی شده و فقط این بخش‌ها را دارد:

- **فروش:** صدور فاکتور با انتخاب کالا، تعداد، مشتری و تخفیف؛ به‌همراه خلاصه فروش
- **سوابق فاکتورها:** جست‌وجو، مشاهده و چاپ فاکتورهای ثبت‌شده
- **تنظیمات:** انتخاب واحد نمایش قیمت‌ها بین ریال، تومان و دلار

فروشنده هیچ دسترسی برای افزودن کالا، تغییر مستقیم موجودی یا تغییر نرخ ارز ندارد. ثبت فاکتور از سیستم مرکزی انجام می‌شود و موجودی همان‌جا کاهش پیدا می‌کند.

## اتصال به سیستم اصلی

ابتدا روی سیستم اصلی، برنامه حساب‌یار را اجرا کنید. برنامه اصلی روی پورت `8000` اجرا می‌شود.

سپس فایل `dist\HesabyarSellerEdit.exe` را روی سیستم فروشنده اجرا کنید. در پنجرهٔ اول، IP سیستم اصلی را وارد کنید؛ برای نمونه:

```text
192.168.1.20
```

سیستم فروشنده و سیستم اصلی باید در یک شبکه باشند و پورت `8000` در Windows Firewall سیستم اصلی اجازهٔ دسترسی شبکهٔ داخلی داشته باشد.

## ساخت فایل اجرایی

پس از نصب وابستگی‌ها، در PowerShell اجرا کنید:

```powershell
.\build_seller_exe.ps1
```

خروجی در مسیر `dist\HesabyarSellerEdit.exe` ساخته می‌شود.
